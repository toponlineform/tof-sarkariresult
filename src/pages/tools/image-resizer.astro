---
import Layout from '../../layouts/Layout.astro';
import AdSlot from '../../components/AdSlot.astro';

// 🔥 SEO KA JAAL: Dynamic Title & Description
const title = "Free Image Resizer & Compressor - Resize to 20kb, 50kb, 100kb Online";
const description = "Best Free Image Resizer for Govt Jobs. Compress JPEG/PNG to 20kb, 50kb, 100kb. Resize photo and signature for SSC, UPSC, IBPS forms with Date Printing feature.";
---

<Layout title={title} description={description}>
  
  {/* External Libraries for Image Processing */}
  <script is:inline src="https://cdnjs.cloudflare.com/ajax/libs/browser-image-compression/2.0.2/browser-image-compression.min.js"></script>

  <div class="max-w-6xl mx-auto px-4 py-8">
      
      {/* HEADER SECTION */}
      <div class="text-center mb-8">
          <h1 class="text-3xl md:text-5xl font-extrabold text-gray-800 mb-3">
              Infinity <span class="text-blue-600">Image Resizer</span>
          </h1>
          <p class="text-gray-500 text-lg max-w-2xl mx-auto">
              Reduce image size in KB/MB, Resize dimensions, and Format for 
              <span class="font-bold text-gray-700"> SSC, UPSC, & Railways</span> forms without quality loss.
          </p>
      </div>

      <div class="flex flex-col lg:flex-row gap-8">
          
          {/* 🎛️ MAIN TOOL AREA */}
          <main class="flex-1">
              <div class="bg-white border-2 border-dashed border-blue-300 rounded-2xl p-6 md:p-10 shadow-sm text-center transition-all" id="dropZone">
                  
                  {/* STEP 1: UPLOAD */}
                  <div id="step1" class="py-10">
                      <div class="w-20 h-20 bg-blue-50 text-blue-500 rounded-full flex items-center justify-center mx-auto mb-4 text-4xl">
                          📂
                      </div>
                      <h3 class="text-xl font-bold text-gray-700 mb-2">Drag & Drop or Select Image</h3>
                      <p class="text-sm text-gray-400 mb-6">Supports JPG, PNG, WEBP (Max 10MB)</p>
                      
                      <input type="file" id="imageInput" accept="image/*" class="hidden" />
                      <button onclick="document.getElementById('imageInput').click()" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-8 rounded-full shadow-lg transform hover:-translate-y-1 transition">
                          Select Image
                      </button>
                  </div>

                  {/* STEP 2: EDITOR (Hidden initially) */}
                  <div id="step2" class="hidden text-left">
                      <div class="flex flex-col md:flex-row gap-6">
                          
                          {/* Preview Area */}
                          <div class="flex-1 flex flex-col items-center">
                              <div class="bg-gray-100 border border-gray-200 rounded p-1 w-full max-w-[300px]">
                                  <img id="previewImg" class="w-full h-auto rounded" />
                              </div>
                              <p class="text-xs text-gray-500 mt-2">Original: <span id="originalSize" class="font-bold text-gray-800">0 KB</span></p>
                          </div>

                          {/* Controls Area */}
                          <div class="flex-1 space-y-6">
                              
                              {/* 🎚️ Compression Slider */}
                              <div>
                                  <label class="block text-sm font-bold text-gray-700 mb-2">
                                      Target File Size (KB)
                                  </label>
                                  <input type="range" id="sizeSlider" min="10" max="500" value="50" class="w-full h-2 bg-gray-200 rounded-lg appearance-none cursor-pointer accent-blue-600" />
                                  <div class="flex justify-between text-xs text-gray-500 mt-1">
                                      <span>10 KB</span>
                                      <span id="targetSizeDisplay" class="font-bold text-blue-600 text-lg">50 KB</span>
                                      <span>500 KB</span>
                                  </div>
                              </div>

                              {/* 📏 Dimensions (Optional) */}
                              <div class="grid grid-cols-2 gap-4">
                                  <div>
                                      <label class="block text-xs font-bold text-gray-500 mb-1">Width (px)</label>
                                      <input type="number" id="widthInput" class="w-full p-2 border rounded focus:border-blue-500 outline-none" placeholder="Auto" />
                                  </div>
                                  <div>
                                      <label class="block text-xs font-bold text-gray-500 mb-1">Height (px)</label>
                                      <input type="number" id="heightInput" class="w-full p-2 border rounded focus:border-blue-500 outline-none" placeholder="Auto" />
                                  </div>
                              </div>

                              {/* 🔘 Action Buttons */}
                              <button id="compressBtn" class="w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg shadow-md flex justify-center items-center gap-2">
                                  <span id="btnText">Compress & Download</span>
                                  <span id="loader" class="hidden animate-spin h-5 w-5 border-2 border-white border-t-transparent rounded-full"></span>
                              </button>
                              
                              <button id="resetBtn" class="w-full text-gray-500 text-sm hover:text-red-500 underline">
                                  Upload Different Image
                              </button>
                          </div>
                      </div>
                  </div>

              </div>

              {/* 👇 SEO TRAP: Content for Keywords */}
              <div class="mt-12 prose max-w-none">
                  <h2 class="text-2xl font-bold text-gray-800">How to Resize Image to 20kb, 50kb for Online Forms?</h2>
                  <p class="text-gray-600">
                      Most government job applications like <strong>SSC CGL, CHSL, UPSC, IBPS, and Railways</strong> require specific file sizes for photographs and signatures. 
                      Typically, the photo size should be between <strong>20kb to 50kb</strong> and the signature between <strong>10kb to 20kb</strong>.
                  </p>

                  <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6 not-prose">
                      <div class="bg-blue-50 p-4 rounded-lg border border-blue-100">
                          <h3 class="font-bold text-blue-800">⚡ Reduce Image Size in KB</h3>
                          <p class="text-sm text-gray-600 mt-1">Compress large MB images to exact KB sizes (20kb, 50kb, 100kb) instantly without losing quality.</p>
                      </div>
                      <div class="bg-green-50 p-4 rounded-lg border border-green-100">
                          <h3 class="font-bold text-green-800">🔒 100% Safe & Secure</h3>
                          <p class="text-sm text-gray-600 mt-1">All processing happens inside your browser. Your photos are never uploaded to any server.</p>
                      </div>
                  </div>

                  <h3 class="text-xl font-bold text-gray-800 mt-8">Common Size Requirements for 2026 Exams:</h3>
                  <div class="overflow-x-auto">
                    <table class="min-w-full text-sm text-left border border-gray-200 mt-2">
                        <thead class="bg-gray-100 font-bold text-gray-700">
                            <tr>
                                <th class="p-2 border">Exam Name</th>
                                <th class="p-2 border">Photo Size</th>
                                <th class="p-2 border">Signature Size</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td class="p-2 border">SSC (CGL, CHSL, MTS)</td>
                                <td class="p-2 border">20kb - 50kb</td>
                                <td class="p-2 border">10kb - 20kb</td>
                            </tr>
                            <tr>
                                <td class="p-2 border">UPSC (IAS, NDA, CDS)</td>
                                <td class="p-2 border">20kb - 300kb</td>
                                <td class="p-2 border">20kb - 300kb</td>
                            </tr>
                            <tr>
                                <td class="p-2 border">IBPS / SBI (Bank PO)</td>
                                <td class="p-2 border">20kb - 50kb</td>
                                <td class="p-2 border">10kb - 20kb</td>
                            </tr>
                            <tr>
                                <td class="p-2 border">Railway (RRB NTPC)</td>
                                <td class="p-2 border">15kb - 40kb</td>
                                <td class="p-2 border">10kb - 20kb</td>
                            </tr>
                        </tbody>
                    </table>
                  </div>
              </div>
          </main>

          {/* SIDEBAR */}
          <aside class="w-full lg:w-[300px] flex-shrink-0">
              <AdSlot type="square" />
              <div class="mt-6 bg-yellow-50 border border-yellow-200 p-4 rounded-lg">
                  <h4 class="font-bold text-yellow-800 mb-2">Need More Tools?</h4>
                  <ul class="space-y-2 text-sm text-gray-700">
                      <li>👉 <a href="/tools/age-calculator" class="text-blue-600 hover:underline">Age Calculator</a></li>
                      <li>👉 <a href="#" class="text-blue-600 hover:underline">PDF Merger (Coming Soon)</a></li>
                  </ul>
              </div>
          </aside>
      </div>
  </div>

  {/* 🧠 JAVASCRIPT MAGIC */}
  <script>
      const imageInput = document.getElementById('imageInput');
      const dropZone = document.getElementById('dropZone');
      const step1 = document.getElementById('step1');
      const step2 = document.getElementById('step2');
      const previewImg = document.getElementById('previewImg');
      const originalSizeSpan = document.getElementById('originalSize');
      const sizeSlider = document.getElementById('sizeSlider');
      const targetSizeDisplay = document.getElementById('targetSizeDisplay');
      const compressBtn = document.getElementById('compressBtn');
      const resetBtn = document.getElementById('resetBtn');
      const btnText = document.getElementById('btnText');
      const loader = document.getElementById('loader');

      let originalFile = null;

      // Handle File Selection
      imageInput.addEventListener('change', (e) => handleFile(e.target.files[0]));

      // Update Slider Text
      sizeSlider.addEventListener('input', (e) => {
          targetSizeDisplay.innerText = e.target.value + " KB";
      });

      function handleFile(file) {
          if (!file) return;
          originalFile = file;
          
          // Show Preview
          previewImg.src = URL.createObjectURL(file);
          originalSizeSpan.innerText = (file.size / 1024).toFixed(2) + " KB";

          // Switch to Editor Mode
          step1.classList.add('hidden');
          step2.classList.remove('hidden');
      }

      // Reset
      resetBtn.addEventListener('click', () => {
          step1.classList.remove('hidden');
          step2.classList.add('hidden');
          imageInput.value = "";
          originalFile = null;
      });

      // 🔥 COMPRESSION LOGIC
      compressBtn.addEventListener('click', async () => {
          if (!originalFile) return;

          // UI Loading State
          btnText.innerText = "Compressing...";
          loader.classList.remove('hidden');
          compressBtn.disabled = true;

          const targetKB = parseInt(sizeSlider.value);
          const options = {
              maxSizeMB: targetKB / 1024, // Convert KB to MB
              maxWidthOrHeight: 1920,
              useWebWorker: true
          };

          try {
              // @ts-ignore (Library loaded via CDN)
              const compressedFile = await imageCompression(originalFile, options);
              
              // Download Trigger
              const link = document.createElement("a");
              link.href = URL.createObjectURL(compressedFile);
              link.download = `TopOnlineForm-Resized-${Date.now()}.jpg`;
              link.click();

              btnText.innerText = "Downloaded! Compress Again";
          } catch (error) {
              console.error(error);
              alert("Something went wrong! Please try again.");
              btnText.innerText = "Try Again";
          } finally {
              loader.classList.add('hidden');
              compressBtn.disabled = false;
          }
      });
  </script>
</Layout>
