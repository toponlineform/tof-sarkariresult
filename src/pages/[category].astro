---
import Layout from '../layouts/Layout.astro';
import AdSlot from '../components/AdSlot.astro'; 
import { allPosts } from '../data/index.js';

export async function getStaticPaths() {
  const categories = [
    { slug: 'latest-jobs', name: 'Latest Jobs' },
    { slug: 'admit-card', name: 'Admit Card' },
    { slug: 'results', name: 'Result' }, 
    { slug: 'answer-key', name: 'Answer Key' },
    { slug: 'admission', name: 'Admission' },
    { slug: 'syllabus', name: 'Syllabus' },
    { slug: 'previous-papers', name: 'Previous Paper' }
  ];

  return categories.map((cat) => {
    const filteredPosts = allPosts.filter(post => {
        const isMainCategory = post.category === cat.name;
        const isTagged = post.types && post.types.includes(cat.name);
        return isMainCategory || isTagged;
    });
    
    filteredPosts.sort((a, b) => {
        const parseDate = (dateStr) => {
            if (!dateStr) return 0;
            const parts = dateStr.split('/');
            return new Date(`${parts[2]}-${parts[1]}-${parts[0]}`).getTime();
        };

        let dateA, dateB;

        if (cat.slug === 'latest-jobs') {
            dateA = parseDate(a.reopenDate || a.postDate);
            dateB = parseDate(b.reopenDate || b.postDate);
        } 
        else if (cat.slug === 'admit-card') {
            dateA = parseDate(a.admitCardDate || a.updatedDate || a.postDate);
            dateB = parseDate(b.admitCardDate || b.updatedDate || b.postDate);
        } 
        else if (cat.slug === 'answer-key') {
            dateA = parseDate(a.answerKeyDate || a.updatedDate || a.postDate);
            dateB = parseDate(b.answerKeyDate || b.updatedDate || b.postDate);
        }
        else if (cat.slug === 'results') {
            dateA = parseDate(a.resultDate || a.updatedDate || a.postDate);
            dateB = parseDate(b.resultDate || b.updatedDate || b.postDate);
        }
        else if (cat.slug === 'syllabus') {
            dateA = parseDate(a.syllabusDate || a.updatedDate || a.postDate);
            dateB = parseDate(b.syllabusDate || b.updatedDate || b.postDate);
        }
        else {
            dateA = parseDate(a.updatedDate || a.postDate);
            dateB = parseDate(b.updatedDate || b.postDate);
        }

        if (dateB !== dateA) return dateB - dateA;
        return (b.id || 0) - (a.id || 0);
    });

    return {
      params: { category: cat.slug },
      props: { categoryName: cat.name, posts: filteredPosts },
    };
  });
}

const { categoryName, posts } = Astro.props;

// Helper: Button Border Color
const getBorderColor = (cat) => {
    const c = cat.toLowerCase();
    if (c.includes('admit')) return 'border-blue-600';
    if (c.includes('result')) return 'border-green-600';
    return 'border-red-600';
};

// --- 🚀 INFINITY SEO DESCRIPTION LOGIC ---
// Har category ke liye alag description jo Google me dikhega
const seoDesc = `Check Latest ${categoryName} 2025 updates. Get direct links for ${categoryName} and online forms for various government exams at TopOnlineForm.com.`;
---

{/* ✅ SEO UPDATE: Passing categoryName and optimized description */}
<Layout 
    title={categoryName} 
    description={seoDesc}
>
  
  <div class="max-w-7xl mx-auto px-2 mb-10">
      
      {/* 1. BREADCRUMB */}
      <nav class="flex text-xs text-gray-500 mb-4 mt-2 px-1 font-medium">
        <a href="/" class="hover:text-blue-700">Home</a> <span class="mx-1">›</span>
        <span class="text-gray-700 font-bold">{categoryName}</span>
      </nav>

      {/* 2. PAGE HEADER */}
      <div class="text-center mb-6">
        <h1 class={`text-2xl md:text-3xl font-extrabold text-gray-800 border-b-4 inline-block pb-2 px-4 ${getBorderColor(categoryName)} uppercase tracking-tight`}>
            {categoryName} <span class="text-red-600">2025</span>
        </h1>
        <p class="text-xs text-gray-400 mt-1 italic uppercase tracking-widest">Fastest Updates on TopOnlineForm.com</p>
      </div>

      <AdSlot type="horizontal" />

      {/* 🏗️ MAIN LAYOUT */}
      <div class="flex flex-col lg:flex-row gap-8 mt-6">
          
          <main class="flex-1 min-w-0">
              {posts.length > 0 ? (
                  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                      {posts.map((post) => (
                          <a href={`/${post.slug}${categoryName === 'Syllabus' ? '#syllabus' : ''}`} class="group block bg-white border border-gray-300 rounded hover:shadow-lg hover:border-blue-400 transition-all duration-200 p-4 relative overflow-hidden">
                              <div class="absolute top-0 right-0 w-2 h-2 bg-gray-200 group-hover:bg-blue-500 transition-colors"></div>
                              <h2 class="text-sm md:text-base font-bold text-blue-800 group-hover:text-red-700 leading-snug">
                                  {post.shortTitle || post.title}
                              </h2>
                              <div class="mt-3 flex items-center justify-between border-t border-gray-100 pt-2">
                                  <div class="text-xs text-gray-500 font-mono">
                                      📅 {post.postDate}
                                  </div>
                                  {categoryName === 'Syllabus' ? (
                                      <div class="text-xs font-bold text-blue-600 bg-blue-50 px-2 py-0.5 rounded uppercase">Syllabus PDF</div>
                                  ) : post.lastDate ? (
                                      <div class="text-xs font-bold text-red-600 bg-red-50 px-2 py-0.5 rounded">Ends: {post.lastDate}</div>
                                  ) : (
                                      <div class="text-xs font-bold text-green-600 bg-green-50 px-2 py-0.5 rounded">Check Now</div>
                                  )}
                              </div>
                          </a>
                      ))}
                  </div>
              ) : (
                  <div class="text-center p-10 bg-gray-50 border border-dashed border-gray-300 rounded">
                      <p class="text-gray-500">No updates found in {categoryName} yet.</p>
                      <a href="/" class="text-blue-600 font-bold hover:underline mt-2 block">Go Home</a>
                  </div>
              )}

              <div class="mt-10">
                 <AdSlot type="horizontal" />
              </div>
          </main>

          <aside class="hidden lg:block w-[300px] flex-shrink-0">
              <div class="sticky top-4 space-y-6">
                  <a href="https://whatsapp.com/channel/0029Vb7TcG06LwHoTXhZKn2D" target="_blank" class="block bg-white border border-green-200 p-4 rounded shadow-sm hover:shadow-md transition text-center group">
                      <div class="w-12 h-12 bg-green-100 text-green-600 rounded-full flex items-center justify-center mx-auto mb-2 group-hover:scale-110 transition">
                          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.768.967-.941 1.165-.173.198-.347.223-.644.074-.297-.149-1.255-.462-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.297-.347.446-.521.151-.172.2-.296.3-.495.099-.198.05-.372-.025-.521-.075-.148-.669-1.611-.916-2.206-.242-.579-.487-.501-.669-.51l-.57-.01c-.198 0-.52.074-.792.372s-1.04 1.016-1.04 2.479 1.065 2.876 1.213 3.074c.149.198 2.095 3.2 5.076 4.487.709.306 1.263.489 1.694.626.712.226 1.36.194 1.872.118.571-.085 1.758-.719 2.006-1.413.248-.695.248-1.29.173-1.414z"/></svg>
                      </div>
                      <div class="font-bold text-green-700">Join WhatsApp Updates</div>
                  </a>

                  <a href="https://t.me/toponlineform" target="_blank" class="block bg-white border border-blue-200 p-4 rounded shadow-sm hover:shadow-md transition text-center group">
                      <div class="w-12 h-12 bg-blue-100 text-blue-500 rounded-full flex items-center justify-center mx-auto mb-2 group-hover:scale-110 transition">
                          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M11.944 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0a12 12 0 0 0-.694 0Zm5.216 8.784-1.76 8.297c-.13.587-.48.73-.974.454l-2.696-1.986-1.3 1.253c-.144.144-.265.265-.54.265l.192-2.738 4.985-4.506c.217-.193-.047-.3-.337-.107l-6.16 3.878-2.656-.83c-.577-.18-.588-.577.12-.852l10.378-4.004c.48-.173.9.117.748.876Z"/></svg>
                      </div>
                      <div class="font-bold text-blue-600">Join Telegram Alerts</div>
                  </a>

                  <AdSlot type="square" />
              </div>
          </aside>
      </div>
  </div>

</Layout>
